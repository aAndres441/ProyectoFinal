import {Employee} from '../pages/employee/model/employee.model';

export class EmployeeSerializer {
     /* fromJson(json: any): Employee {
        const employee: Employee = {
            id: json.id,
            nombre : json.nombre,
            imagen: json.imagen,
            descripcion: json.descripcion,
            tmstmp : json.tmstmp
        };
        return employee;
    }  */
}
